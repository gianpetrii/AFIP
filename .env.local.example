# Copia este archivo a .env.local si agregás configuración por entorno
# cp .env.local.example .env.local
#
# El extractor AFIP actual no requiere variables de entorno obligatorias.
# Las credenciales de contribuyentes van en el CSV de entrada (no subir al repo).
